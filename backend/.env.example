# Set to True once your PostgreSQL database and credentials are ready
USE_POSTGRES=False

# PostgreSQL Connection Settings
DB_NAME=nexcampus_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=localhost
DB_PORT=5432

# CORS & Debug
DEBUG=True
CORS_ALLOW_ALL=True
